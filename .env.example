# Copy this file to .env and fill in your real values.
# Do not commit .env (it contains secrets).

OPENWEATHER_API_KEY=your_openweather_api_key_here

# AWS credentials are required for Claude (Bedrock). Configure via:
#   aws configure
# or set AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY in this file or your environment.
